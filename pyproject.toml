[project]
name = "benchwarmer"
version = "0.1.0"
description = "Portable evaluations for comparing model utility and economics"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.20.0",
    "pydantic>=2.13.5",
    "sqlalchemy>=2.0.53",
]

[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]

[tool.pyright]
executionEnvironments = [
    { root = ".", extraPaths = ["src"] },
]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.format]
indent-style = "space"
quote-style = "double"
